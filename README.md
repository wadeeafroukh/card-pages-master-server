# Card Pages Manager – Server (Express)

Server-side (REST API) for the **Card Pages Manager** project.  
Built with **Node.js + Express + MongoDB (Mongoose)** and includes authentication & authorization using **JWT**, password hashing with **bcrypt**, validation using **Joi**, and utility helpers with **lodash**.

---

## ✨ Features

- **Express REST API**
- **MongoDB + Mongoose** models (Users / Cards)
- **Auth middleware** (protected routes)
- **JWT Authentication**
- **bcrypt** password hashing
- **Joi** request validation
- **CORS** enabled
- Environment variables via **dotenv**

---

## 🧱 Tech Stack

- Node.js
- Express
- MongoDB / Mongoose
- JSON Web Token (JWT)
- bcrypt
- Joi
- lodash

---

## 📁 Project Structure